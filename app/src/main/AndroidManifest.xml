<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aplicativo_de_cadastros">

    <!-- Permissões de rede -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aplicativo_de_Cadastros">

        <!-- Atividade principal do aplicativo (MainActivity) -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Atividade para adicionar um item (AddItemActivity) -->
        <activity android:name=".AddItemActivity" />

        <!-- Atividade para visualizar os detalhes de um item (ViewItemActivity) -->
        <activity android:name=".ViewItemActivity" />

        <!-- Atividade para editar um item existente (EditItemActivity) -->
        <activity android:name=".EditItemActivity" />

        <!-- Atividade para confirmação de exclusão (DeleteConfirmationActivity) -->
        <activity android:name=".DeleteConfirmationActivity" />

    </application>
</manifest>
